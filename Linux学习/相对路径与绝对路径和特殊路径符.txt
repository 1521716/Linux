1、相对路径和绝对路径
	绝对路径：以根目录做七点，描述路径的方式，路径以/开头
	相对路径：以当前目录为起点，描述路径的方式，路径不需要以/开头

2、特殊路径符
	.表示当前目录，比如cd .或者cd ./Desktop
	..表示上一级目录，比如cd ..（上一级）或cd ../..（上两级）
	~表示用户的HOME目录，比如cd ~或者cd ~/Desktop      (即cd /home/用户名)