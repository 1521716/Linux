1、Linux系统的超级管理员用户是：root用户

2、su命令
	可以切换用户，语法： su [-] [用户名]
	-表示切换后加载环境变量（以后讲），建议带上
	用户名可以省略，默认切换到root用户
	切换用户后，可以通过命令：exit 退回上一个用户，也可以使用快捷键CTRL+D
	普通用户切换到其他用户需要密码，root用户切换到其他用户不需要密码

3、 sudo命令
	可以让一条普通命令带有root权限，语法：sudo 其他命令
	需要以root用户执行visudo命令，增加配置方可以让普通用户有sudo命令的执行权限
	方法详见网络上视频
	