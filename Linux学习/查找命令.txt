查找命令
	1、which
		用于查找命令的程序文件
		语法：which 要查找的命令
		无需选项，只需要参数表示要查找哪个命令

	2、find
		用于查找指定文件
		按照文件名查找：find 起始路径 -name "要查找的文件名"                    支持通配符*
		按照文件大小查找：find 起始路径 -size -|+n[kMG]                       -表示小于，+表示大于，n为大小数字，k(小写)为单位kb，M（大写）为单位MB，G（大写）为单位GB
